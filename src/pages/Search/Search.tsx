import React from 'react';

export default function Search() {
  //pegar a url com window.location.search
  //dividir com split('=')
  //verificar se o primeiro item é igual a 'product' ou 'category'
  //usar as funções utils para pegar ps produtos de acordo com o id buscado
  //verificar filtros
  return <div>Search</div>;
}
